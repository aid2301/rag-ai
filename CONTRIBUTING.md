# 贡献指南

1. 按 README 启动开发环境，为一项具体改动创建分支。
2. 业务规则放在 backend/app/services，HTTP 路由负责参数与权限；提示词放在 prompts。
3. 前端 API 按领域放在 frontend/src/api，通过 api.ts 对外导出。共享 UI 放在 components。
4. 行为变化应补充能验证用户结果的测试，并运行 README 中的后端测试、前端测试、Lint 和构建。
5. PR 说明问题、结果与验证方式。涉及配置或部署时同步更新 .env.example 和文档。

请勿提交真实企业文档、聊天记录、模型密钥、环境文件、数据库备份或运行日志。示例请使用虚构数据。不要把令牌或真实资料粘贴到公开 Issue。

提交贡献即表示你有权以本项目 MIT 许可证提供这些内容。
